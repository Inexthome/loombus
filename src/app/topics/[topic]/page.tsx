import TopicSignalClient from "./topic-signal-client";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function TopicSignalPage() {
  return <TopicSignalClient />;
}
