import TransparencyReportingClient from "./transparency-reporting-client";

export const metadata = {
  title: "Transparency Reporting | Loombus Admin",
  robots: { index: false, follow: false },
};

export default function TransparencyReportingPage() {
  return <TransparencyReportingClient />;
}
