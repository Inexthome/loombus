name: Professional Booking Price History Editorial UI

on:
  pull_request:
    paths:
      - "src/components/professional-booking-price-history-card.tsx"
      - "src/app/appointments/professional-pricing-history/page.tsx"
      - "scripts/verification/verify-professional-booking-price-history-editorial-ui.mjs"
      - ".github/workflows/professional-booking-price-history-editorial-ui.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Verify Professional Booking Price History Editorial UI
        run: node scripts/verification/verify-professional-booking-price-history-editorial-ui.mjs
