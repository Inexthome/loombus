name: Login Editorial UI

on:
  pull_request:
    paths:
      - "src/app/login/page.tsx"
      - "src/app/login/layout.tsx"
      - "scripts/verification/verify-login-editorial-ui.mjs"
      - ".github/workflows/login-editorial-ui.yml"
  push:
    branches:
      - main
    paths:
      - "src/app/login/page.tsx"
      - "src/app/login/layout.tsx"
      - "scripts/verification/verify-login-editorial-ui.mjs"
      - ".github/workflows/login-editorial-ui.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Verify Login Editorial UI
        run: node scripts/verification/verify-login-editorial-ui.mjs
