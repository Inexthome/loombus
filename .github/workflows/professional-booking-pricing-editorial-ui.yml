name: Professional Booking Pricing Editorial UI

on:
  pull_request:
    paths:
      - "src/components/professional-booking-pricing-card.tsx"
      - "src/app/appointments/professional-pricing/page.tsx"
      - "scripts/verification/verify-professional-booking-pricing-editorial-ui.mjs"
      - ".github/workflows/professional-booking-pricing-editorial-ui.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Verify Professional Booking Pricing Editorial UI
        run: node scripts/verification/verify-professional-booking-pricing-editorial-ui.mjs
