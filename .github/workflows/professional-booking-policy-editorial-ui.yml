name: Professional Booking Policy Editorial UI

on:
  pull_request:
    paths:
      - "src/components/professional-booking-policy-card.tsx"
      - "src/app/appointments/professional-policy/page.tsx"
      - "scripts/verification/verify-professional-booking-policy-editorial-ui.mjs"
      - ".github/workflows/professional-booking-policy-editorial-ui.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Verify Professional Booking Policy Editorial UI
        run: node scripts/verification/verify-professional-booking-policy-editorial-ui.mjs
