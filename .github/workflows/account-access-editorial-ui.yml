name: Account Access Editorial UI

on:
  pull_request:
    paths:
      - "src/app/account-access/page.tsx"
      - "scripts/verification/verify-account-access-editorial-ui.mjs"
      - ".github/workflows/account-access-editorial-ui.yml"
  push:
    branches:
      - main
    paths:
      - "src/app/account-access/page.tsx"
      - "scripts/verification/verify-account-access-editorial-ui.mjs"
      - ".github/workflows/account-access-editorial-ui.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Verify Account Access Editorial UI
        run: node scripts/verification/verify-account-access-editorial-ui.mjs
