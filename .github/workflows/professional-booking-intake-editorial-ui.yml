name: Professional Booking Intake Editorial UI

on:
  pull_request:
    paths:
      - "src/app/appointments/professional-intake/page.tsx"
      - "src/components/professional-booking-intake-card.tsx"
      - "scripts/verification/verify-professional-booking-intake-editorial-ui.mjs"
      - ".github/workflows/professional-booking-intake-editorial-ui.yml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Verify Professional Booking Intake Editorial UI
        run: node scripts/verification/verify-professional-booking-intake-editorial-ui.mjs
